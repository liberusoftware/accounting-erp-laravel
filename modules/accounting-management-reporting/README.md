# Accounting Management Reporting

Owns tenant-scoped management report packs and their narratives, charts, schedules, review/approval workflow, delivery artifacts, and archive lifecycle. Source data is represented by opaque references so the module does not couple to another module's private storage.
