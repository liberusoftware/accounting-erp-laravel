# Accounting Migration Framework

Owns migration source inventory, field mappings, dry runs, transforms, validation, batches, resumability, attachments, row errors, counts, and reconciliation. Batch execution is deliberately contract-based: source records and destination records are opaque references and no application models are required.
