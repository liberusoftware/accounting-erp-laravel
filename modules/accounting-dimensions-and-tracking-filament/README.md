# Accounting Dimensions and Tracking Filament

Optional Filament presentation adapter for accounting dimensions.

Attach `DimensionsFilamentPlugin::make()` to register the resource. Create and
edit pages delegate to `SaveDimension`.
