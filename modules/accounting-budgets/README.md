# Accounting Budgets

Provider-neutral budgets for annual and rolling plans, phased lines, versions, approvals, revisions, and actual comparison. The module owns its tables and revalidates lifecycle invariants at every action boundary.
