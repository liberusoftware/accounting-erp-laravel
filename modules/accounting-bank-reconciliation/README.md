# Accounting Bank Reconciliation

Owns reconciliation sessions, suggested/exact match decisions, transfers, grouped receipts, fees, interest, adjustments, exceptions, statement balances, variance, and sign-off. Source and bank-feed identifiers are opaque references so the module does not access another module's private storage.
