# Accounting Three-Way Matching

Provider-neutral matching of purchase orders, receipts, and supplier bills. Source records are referenced by opaque type/id pairs and evaluated from captured quantity, price, and tax snapshots. The module owns tolerance checks, blocking and warning exceptions, partial matching, approval overrides, rejection, and immutable evidence snapshots.
