<div class="space-y-4"><div class="flex items-center justify-between"><h1 class="text-xl font-semibold">Tax rules</h1><select wire:model.live="status" class="rounded border"><option value="">All statuses</option><option value="draft">Draft</option><option value="active">Active</option><option value="archived">Archived</option></select></div><div wire:loading class="text-sm text-gray-500">Loading…</div><ul>@forelse($rules as $rule)<li wire:key="tax-rule-{{ $rule->id }}" class="flex justify-between border-b py-2"><span>{{ $rule->code }} — {{ $rule->name }}</span><span>{{ $rule->rate }}% · {{ $rule->status->value }}</span></li>@empty<li>No tax rules found.</li>@endforelse</ul>{{ $rules->links() }}</div>
