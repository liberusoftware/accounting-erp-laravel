# Accounting Periods API

Authenticated Sanctum API for period creation, lifecycle transitions, locks,
posting-date decisions, and reopen evidence.

Routes are registered below `/api/v1/accounting/accounting-periods`. The
OpenAPI 3.1 fragment is stored at `openapi/v1/accounting-periods.yaml`.
