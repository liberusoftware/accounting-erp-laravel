# Accounting Spreadsheet Migration

Owns reusable templates, guided mappings, opening/outstanding/detail modes, duplicate source controls, balancing, validation, and reconciliation runs. The domain boundary is `CreateMigrationTemplate`, `CreateMigrationRun`, and `ValidateMigration`; imported row data remains untrusted payload rather than an application model dependency.
