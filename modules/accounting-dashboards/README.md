# Accounting Dashboards

Owns tenant-scoped dashboards, role-specific KPIs, period and dimension context, targets, drill-through metadata, refresh timestamps, and controlled sharing. Presentation packages delegate to this domain boundary.
