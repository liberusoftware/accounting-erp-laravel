# Accounting Sales Orders

Owns accepted demand, allocation/fulfillment references, deposits, partial invoicing, cancellation/status transitions, and opaque billing/ecommerce references. Legacy estimate conversion is represented by `estimate_id` without coupling this package to application models.
