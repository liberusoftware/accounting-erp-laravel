# Accounting Sales Tax and GST

Owns registrations/nexus, destination/origin context, taxable bases, liabilities, adjustments, return periods, and calculation records without coupling to an application or provider SDK. Use `CreateSalesTaxRecord`, `ActivateSalesTaxRecord`, `CloseSalesTaxRecord`, and `SalesTaxRecordQuery` as the public boundary.
