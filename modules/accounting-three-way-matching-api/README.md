# Accounting Three-Way Matching API

Sanctum-protected endpoints are under `/api/v1/accounting/three-way-matching`; read operations require `accounting.three-way-matching.read` and mutations require `accounting.three-way-matching.write`.
