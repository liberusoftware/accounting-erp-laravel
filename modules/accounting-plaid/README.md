# Accounting Plaid

Plaid is an infrastructure adapter. It implements the `BankFeedAdapter` contract and normalizes Plaid's signed/debit, pending, modified, and removed transaction payloads for the provider-neutral Bank Feeds module. Plaid SDK/client concerns remain in this package.
