# Accounting Projects and Jobs

Owns customer references, project/job hierarchy, dates, managers, budgets, dimensions, source links, and lifecycle. It preserves the legacy project name/code/date/status/team semantics without coupling to application project models.
