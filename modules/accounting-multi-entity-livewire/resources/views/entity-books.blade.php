<div class="space-y-4"><h1 class="text-xl font-semibold">Entity books</h1><select wire:model.live="status" class="rounded border"><option value="">All statuses</option><option value="active">Active</option><option value="suspended">Suspended</option><option value="archived">Archived</option></select><div wire:loading>Loading…</div><ul>@forelse($entities as $entity)<li wire:key="entity-book-{{ $entity->id }}" class="border-b py-2">{{ $entity->code }} · {{ $entity->name }} · {{ $entity->base_currency }} · {{ $entity->status->value }}</li>@empty<li>No entity books found.</li>@endforelse</ul>{{ $entities->links() }}</div>
