# Accounting Bank Accounts Livewire

The `module-accounting-bank-accounts::accounts` component presents account
balances and lifecycle state through the public bank-account query boundary.
It requires authentication and never renders restricted account details.
