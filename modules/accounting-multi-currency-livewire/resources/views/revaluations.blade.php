<div class="space-y-4"><h1 class="text-xl font-semibold">Currency revaluations</h1><div wire:loading>Loading…</div><ul>@forelse($runs as $run)<li wire:key="currency-revaluation-{{ $run->id }}" class="border-b py-2">{{ $run->run_ref }} · {{ $run->as_of_date }} · {{ $run->functional_currency }} · {{ $run->status->value }} · unrealized {{ $run->unrealized_gain }} / {{ $run->unrealized_loss }}</li>@empty<li>No revaluations found.</li>@endforelse</ul>{{ $runs->links() }}</div>
