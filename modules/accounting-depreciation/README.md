# Accounting Depreciation

Owns tenant-scoped depreciation methods, conventions, useful life, residual value, schedules, calculated and posted runs, journal references, tax books, and forecast read models. Asset identifiers remain opaque so this package does not depend on a host asset register.
