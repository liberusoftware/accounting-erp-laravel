<div class="space-y-4"><h2 class="text-lg font-semibold">Branches and locations</h2><ul class="divide-y">@forelse($branches as $branch)<li class="py-2"><span class="font-medium">{{ $branch->code }} — {{ $branch->name }}</span><span class="text-sm text-gray-500">{{ $branch->location }} · {{ $branch->status }}</span></li>@empty<li class="py-2">No branches found.</li>@endforelse</ul></div>
