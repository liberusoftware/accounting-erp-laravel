# Accounting Multi-Currency

Owns currency-role configuration, provider-neutral historical rates, revaluation runs, realized/unrealized gain evidence, and reconciliation. Existing application currency services remain compatible integrations; this module does not depend on their private models.
