# Accounting Multi-Entity

Owns independently installable entity books and their access, switching, master-data policies, periods/tax settings, mappings, and reporting summaries. Legal-entity and user identifiers are opaque integration references; no application model or private module table is required.
