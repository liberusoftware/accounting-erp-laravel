# Accounting Payment Reconciliation API

Authenticated, throttled, versioned adapter for the payment reconciliation public boundary at `/api/v1/accounting/payment-reconciliation`. Controllers validate transport shape and delegate domain mutations; resources expose explicit attributes rather than serializing models automatically.
