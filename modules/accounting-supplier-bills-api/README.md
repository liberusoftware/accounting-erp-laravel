# Accounting Supplier Bills API

Authenticated Sanctum endpoints live under `/api/v1/accounting/supplier-bills`. The OpenAPI 3.1 fragment is `openapi/v1/accounting-supplier-bills.yaml`; writes require `accounting.supplier-bills.write`, reads require `accounting.supplier-bills.read`.
